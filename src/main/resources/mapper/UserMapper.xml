<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mybatis_study.mapper.UserMapper">
    <select id="selectList" resultType="org.example.mybatis_study.pojo.User">

        SELECT *
        FROM user
        <where>
            is_deleted = 0
            <if test="name!= null">
                And nick_name LIKE CONCAT('%', #{name}, '%')
            </if>
            LIMIT #{pageNum}, #{pageSize}
        </where>
    </select>
    <select id="selectById" resultType="org.example.mybatis_study.pojo.User">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO user(username, password, dept_id)
        VALUES (#{username}, #{password}, #{deptId})
    </insert>

    <update id="updateById">
        UPDATE user
        SET username = #{username},
            dept_id  = #{deptId}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE user
        SET is_deleted = 1
        WHERE id = #{id}
    </update>
    <update id="deleteBypartId">
        UPDATE user
        SET is_deleted = 1
        WHERE dept_id = #{id}
    </update>
    <select id="getLogin" resultType="org.example.mybatis_study.pojo.User">
        SELECT * FROM user
        WHERE
            user_name = #{username}
        AND
            password = #{password}
    </select>
</mapper>